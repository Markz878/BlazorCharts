@page "/stackedbarchart"

<PageTitle>Stacked Bar Chart</PageTitle>

@if(data is not null)
{
    <StackedBarChart Data="data"/>
}

@code {
    private List<StackedBarColumn>? data;

    protected override void OnInitialized()
    {
        data = Enumerable.Range(0,5).Select(x=>new StackedBarColumn($"Item {x + 1}", new List<StackedBarItem>() { new StackedBarItem((x + 1) * 100, "red"), new StackedBarItem((x + 1) * 200, "yellow") })
        {
            //TooltipProperties = new Dictionary<string, string>
            //{
            //    {"Energian kulutus", $"{Random.Shared.Next(50,250)} kWh/m2"},
            //    {"Elinkaarikustannukset", $"{Random.Shared.Next(100,1000)} 000 t€"},
            //    {"Muuttuja 1", $"{Random.Shared.Next(100,1000)}"},
            //    {"Muuttuja 2", $"{Random.Shared.Next(100,1000)}"},
            //    {"Muuttuja 3", $"{Random.Shared.Next(100,1000)}"},
            //}
        }).ToList();
    }
}
